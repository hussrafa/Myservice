name: dotnet package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write

    steps:
      - uses: actions/checkout@v4
      - name: Setup dotnet 
        uses: actions/setup-dotnet@v3

      - name: Build
        run: dotnet build Myservice.sln
      
      - name: publish
        run: dotnet publish -c Release Myservice/Myservice.csproj
      
      - name: release
        uses: actions/create-release@v1
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          draft: false
          prerelease: false
          release_name: v1.0.1
          tag_name: v1.0.1
          body: |
            Changes in this Release
            - First Change
            - Second Change
      
      - name: upload  artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: /home/runner/work/Myservice/Myservice/Myservice/bin/Release/net8.0/publish/Myservice.exe
          asset_name: Myservice.exe
          asset_content_type: application/gzip
